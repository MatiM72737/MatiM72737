name: Metrics

on:
schedule:
- cron: "0 */12 * * *"
workflow_dispatch:

jobs:
metrics:
runs-on: ubuntu-latest
steps:
- uses: lowlighter/metrics@v3
with:
token: ${{ secrets.METRICS_TOKEN }}
user: MatiM72737
